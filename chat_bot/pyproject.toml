[project]
name = "chat-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Muhammad Fasih", email = "programmer.fasih.18@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "google-generativeai>=0.8.4",
    "python-dotenv>=1.0.1",
    "streamlit>=1.42.2",
]

[project.scripts]
chat-bot = "chat_bot:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
