[project]
name = "unit-convertor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Muhammad Fasih", email = "programmer.fasih.18@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.42.2",
]

[project.scripts]
unit-convertor = "unit_convertor:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
